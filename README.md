// คำสัง complie ts เป็น js

tsc src/scripts/*.ts --outDir dist/js
tsc src/scripts/*.ts --outDir dist/js --watch
// คำสั่ง run Tailwind css

npm run dev
//คำสั่ง git

git remote -v --> { ชื่อ github ต้องเป็น {https://github.com/Deaw23456/ProJect_REQ.git} }
git add .
git commit -m "ในฟันหนูนี้แล้วแต่จะใส่"
git push origin main
//หาก Test localStorage แล้วข้อมูลไม่แสดง

npx tsc src/scripts/*.ts --outDir dist/js --module ESNext --target ES6 --watch
npx tsc src/scripts/*.ts --outDir dist/js --module ESNext --target ES6
//**** สำคัญ ****\

"หากเจอ erorr ที่ไม่รู้จักให้ถาม AI นะจีะ" "GOOD LUCK จ้าาาา"